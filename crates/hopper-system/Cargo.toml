[package]
name = "hopper-system"
version = "0.3.2"
publish = ["crates-io"]
edition = "2021"
license = "Apache-2.0"
authors = ["QuarksBlueFoot <quark@bluefoot.tech>"]
description = "Hopper-owned System Program builder surface. Thin, no_std instruction builders over Hopper Runtime."
repository = "https://github.com/BluefootLabs/Hopper-Solana-Zero-copy-State-Framework"
homepage = "https://hopperzero.dev"
documentation = "https://docs.rs/crate/hopper-system/0.3.2"
readme = "README.md"
keywords = ["solana", "system-program", "cpi", "zero-copy", "hopper"]
categories = ["no-std"]

[lib]
name = "hopper_system"

[dependencies]
hopper-runtime = { workspace = true }

[features]
default = []

[lints]
workspace = true
