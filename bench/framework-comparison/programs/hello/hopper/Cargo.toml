[package]
name = "hello-hopper"
version = "0.0.0"
edition = "2021"
license = "Apache-2.0"
publish = false
description = "pina framework-comparison hello fixture on the Hopper macro path."

[lib]
crate-type = ["cdylib"]

[dependencies]
hopper = { workspace = true, features = ["proc-macros"] }

[lints]
workspace = true
