[package]
name = "hopper-checked-cpi-fixture"
version = "0.0.0"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
hopper-native = { path = "../../../crates/hopper-native", features = ["cpi"] }

[lints]
workspace = true
