Source review, 2026-09-26. These findings are design tradeoffs, not a security audit or a claim of exploitable vulnerabilities.

Squads v4 af94153ff77a28b6effe46b9c94baaa93742b48c:
- programs/squads_multisig_program/src/instructions/vault_transaction_execute.rs explicitly permits previously approved transactions after they become stale; checks approval timelock but has no generic expiry timestamp. Application instructions can independently impose deadlines and price limits.
- Execution requires a member with Execute permission. This does not mean automation is impossible: bots can be authorized members and the product has limit-order integrations.
- state/spending_limit.rs explicitly defines delegates independently of multisig members; removing a member does not remove its spending-limit authority. destinations=[] permits any destination. This is intentional configurable behavior.
- spending_limit_use.rs is a token/SOL transfer facility, not a generic protocol-specific perps strategy policy.
- Proposal approval/cancellation vectors support votes across transactions; Hopper's new example does not provide that workflow or arbitrary vault-message execution.

Hopper implementation response: threshold-approved SOL payout accounts pin recipient, amount, window, single-use state, and a policy revision. Add/remove-member and threshold changes advance that revision, as does explicit invalidation. A threshold can revoke/reclaim a receipt. Execution is permissionless within those exact limits, without granting a keeper general signing authority. No global-benchmark or universal-superiority claim follows.

Pinocchio head adbd48d12229ffa30d6fb3d3a8ff777fdb053b80 is unchanged from the previous review. sdk/Cargo.toml delegates account and CPI representations to solana-account-view and solana-instruction-view; its lazy entrypoint leaves duplicate handling to its caller. Hopper retains its own native boundary; using a lazy API alone is not a new invention.

Agave head f77165963c709934e8ea8564187c00f081b597d1; SIMD head 4b643ca8746742183a469681765e694b385bb315. Read the current release record for cluster observations rather than inferring availability from proposal status or a source revision. Fresh finalized RPC reports Alpenglow active on devnet (504144000) and testnet (444620256), absent on mainnet.

Local Tally/Tribe review: program/src/processor.rs CreateSplit/DistributeSplit and program/src/savings.rs configured destinations, amount arithmetic, and fee checkpointing. These are application examples of binding financial outcomes. They are not multisig voting protocols. Source was read locally; no private repository code or keys are reproduced here.

Native fixes: specialized unsigned System/token CPI builders now preflight missing signatures; PDA derivation remains the SVM's responsibility. A native-only SBF fixture exposed experimental inline assembly in native abort paths and compute-burning panic handling in the framework. Both now use the SVM abort syscall. This is a concrete compatibility/failure-cost correction, not a claim that abort syscalls are novel.

Pina 478ae3d2811ea7f7869d2a32e6095a9727d4f9d8: the multisig teaching example already implements proposal expiry, stale-index invalidation, individual approval revocation, and role permissions. Those controls are not claimed as Hopper inventions. Hopper's fixed SOL payout example does not match its full proposal/vault-message workflow.

Anchor v2 must be tracked on anchor-next, currently 038c193a9eedd5ffd4aaf831d501a74a0c90084e. The delta reviewed against the previous v2 pin concerns the TypeScript account namespace and @solana/kit. Default master is a different line and must not be reported as the v2 head. Quasar remains b0de7db4cd271654a2dcf78807dd865e98e0b339.

Further native correction: invoke_and_read<T> previously retrieved a snapshot without checking T or the producing program. A direct callee that leaves nested CPI return data behind could therefore be mistaken for the producer. The helper now checks both producer and typed prefix; a three-program SBF regression reproduces the old acceptance and checks the rejection after the fix. Raw get_return_data remains intentionally untrusted until a caller checks provenance and payload meaning.
